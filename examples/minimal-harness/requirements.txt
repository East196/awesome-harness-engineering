# Minimal Harness 依赖
# 安装: pip install -r requirements.txt

# 核心依赖
anthropic>=0.18.0
openai>=1.0.0
pyyaml>=6.0
python-dotenv>=1.0.0
outlines>=0.0.67  # 结构化输出（JSON Schema 约束）

# 异步支持
aiohttp>=3.9.0
asyncio>=3.4.3

# 工具
rich>=13.0.0  # 美化输出
typer>=0.9.0  # CLI 支持

# 开发依赖（可选）
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
